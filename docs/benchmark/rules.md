Este directorio contiene las tomas de tiempo de ejecucion para cada kernel. Crear un nuevo archivo formato <kernel>.md para cada kernel. Dentro del archivo crear una tabla por cada tipo de toma de tiempo.Dentro de cada tabla indicar los tamaños de entrada de las matrices del kernel, el threshold, y el orden maximo alcanzado. Las tomas de tiempo pueden ser obtenidas solo por ncu por ahora. Proximamente se debe crear un metodo solido para medir el tiempo de ejecucion de la operacion entera (contemplando wrappers) tanto en c++ como en python. 